# ─── Compilación (cualquier carpeta build, a cualquier profundidad) ───
build/
Build/
build-*/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
install_manifest.txt
Makefile
*.o
*.a
*.so
*.dylib

# ─── Datos de fantomas (se descargan o se piden aparte) ───
ICRPdata/
*.g4dcm
*.g4pdcm
*.dcm
# ICRP145 / MRCP (mallas tetraédricas)
*.node
*.ele
*.material

# ─── Mapas de campo (regenerables desde el código) ───
*.fld
*.map
field/maps/
# Excepcion: los .map "bendecidos" de produccion (y su GDML de bobinas),
# curados a mano en field/production/ -- para que clonar el repo alcance
# para correr run_organ_sweep.py sin instalar Elmer/Gmsh ni regenerar nada
# (ver field/production/README.md). No confundir con field/generated/,
# que sigue siendo scratch/regenerable y NO se versiona.
!field/production/**

# ─── Resultados de simulación (usa siempre esta carpeta) ───
output/
results/
*.root
*.hepmc
*.csv
# Excepcion: CSV de espectros de energia POR FUENTE (SPENVIS, OLTARIS, etc.)
# en data/sources/<fuente>/*.csv -- esa es la fuente de verdad que SI se
# versiona. data/*.csv (nivel directo) NO se versiona a proposito: es el
# destino generado por scripts/select_spectrum_source.py (copia de la fuente
# activa), analogo a build/ -- versionarlo duplicaria contenido y generaria
# diffs de git cada vez que alguien cambia de fuente activa.
!geant4/**/data/sources/**/*.csv
# Excepcion: resultados finales del barrido (crudos por persona + agregado
# con estadistica), SI se versionan -- son los datos que van al articulo
!geant4/**/resultados/*.csv

# ─── ANSYS ───
*.aedtresults/
*.aedb/
*.lock*
*.asol
*.aedt.batchinfo/

# ─── Python (análisis) ───
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# ─── LaTeX ───
*.aux
*.log
*.bbl
*.blg
*.out
*.synctex.gz
*.fdb_latexmk
*.fls
*.toc

# ─── Sistema / editor ───
.DS_Store
.vscode/
.idea/
*.swp

# Regenerable FEM/CAD conversion outputs; source geometries and material JSON stay tracked.
/field/generated/
/field/wheelhouse/

# Pointer file written by scripts/install.sh --with-elmer (local machine path,
# not portable/versionable -- Elmer itself installs outside the repo).
/field/.elmer-prefix
